import { AutoDetectOtpDirective } from './auto-detect-otp.directive';

describe('AutoDetectOtpDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoDetectOtpDirective();
    expect(directive).toBeTruthy();
  });
});

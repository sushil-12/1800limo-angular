import { EscapeAllStringPipe } from './escape-all-string.pipe';

describe('EscapeAllStringPipe', () => {
  it('create an instance', () => {
    const pipe = new EscapeAllStringPipe();
    expect(pipe).toBeTruthy();
  });
});

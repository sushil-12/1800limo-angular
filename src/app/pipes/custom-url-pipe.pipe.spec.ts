import { CustomUrlPipePipe } from './custom-url-pipe.pipe';

describe('CustomUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});

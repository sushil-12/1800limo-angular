import { BookingStatusTextReplacePipe } from './booking-status-text-replace.pipe';

describe('BookingStatusTextReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new BookingStatusTextReplacePipe();
    expect(pipe).toBeTruthy();
  });
});

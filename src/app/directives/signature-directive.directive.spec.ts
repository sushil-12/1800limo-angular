import { SignatureDirectiveDirective } from './signature-directive.directive';

describe('SignatureDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SignatureDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
